# Reduce install overhead on memory-constrained VPS hosts.
# Browser skip flags: PUPPETEER_SKIP_DOWNLOAD and PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD
# are set in lpad.config.json env for deploy.
fund=false
audit=false
prefer-offline=true
