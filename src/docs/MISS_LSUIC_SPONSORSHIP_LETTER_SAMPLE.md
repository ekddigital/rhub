# Miss LSUIC — Programme Sponsorship Letter (Composer Reference)

Canonical runtime copy lives in `src/lib/conf/fundraising-letter-template.ts` (`buildMissLsuicSponsorshipLetterBodyRichHtml`).
The Letter Composer version uses committee line items plus a programme support snapshot (area + basis rows, total only) instead of quoting a single “target sponsorship amount.” Programme sponsor table tiers and sponsor visibility bullets stay editable via the sidebar.

**Letter Composer:** Fundraising mode → Letter version **Miss LSUIC Pageant & Achievers Night** → **Load sample letter**.

**Scripting / offline sample:** `scripts/conference-2026/miss-lsuic/fundraising-sponsorship-letter-sample.md`

**Letterhead parts (Word / Google Docs):** `/tools/conf/letterhead`
